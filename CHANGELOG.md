# [v2.2.3] - 核心能力统一与体验升级

* **🧩 指令与触发机制优化**
  * 新增并统一核心指令：`/收录`、`/语录`、`/删除`、`/一键金句`
  * 支持 `ignore_prefix` 无前缀触发（可直接发送“语录/收录/删除/一键金句”）
  * 增强命令识别与冲突规避：自动识别其他插件命令，避免误收录/误触发
  * 增加短时间消息去重消费机制，降低重复处理概率

* **📥 语录收录链路增强**
  * 收录流程改为“回复消息”模式，自动拉取原消息详情
  * 收录前新增多重过滤：命令消息、链接消息、机器人自身、黑名单用户
  * 引入 O(1) 哈希索引去重（`group + text`），大规模语录下仍可快速判重
  * 持久化写入采用“临时文件 + 原子替换”，降低数据损坏风险

* **🃏 随机抽卡与批量展示升级**
  * `/语录` 支持单条、批量（如 `/语录 5`）、按人筛选（如 `/语录 @某人`）
  * 支持“自己”语义识别（如 `/语录 自己`）
  * 批量抽取支持合成长图展示，单条抽取支持序号与总量显示
  * 新增 `max_batch_count` 限制，防止一次生成过长图片

* **🤖 AI 一键金句能力完善**
  * 新增 `/一键金句`：自动回溯群聊并挖掘可入库金句
  * 支持目标用户分析（`@某人` / `自己`）与全群分析两种模式
  * LLM Provider 选择支持多级回退：指定配置 → 会话模型 → 首个可用模型
  * AI 入选理由统一沉淀到评论区（`comments`），展示更自然
  * 内置 JSON 容错解析（去代码块、正则提取、`literal_eval` 回退）

* **🧱 OneBot 服务层稳定性提升**
  * 历史消息拉取适配 NapCat/Go-CQHTTP 参数差异
  * 回溯过程加入异常熔断、跳跃回退与节流，减少风控/失败影响
  * 用户名解析采用“群名片 → 陌生人昵称 → QQ号”多级兜底策略
  * 群名查询增加缓存，减少重复 API 调用

* **🎨 渲染系统升级（高清卡片）**
  * 使用 Playwright 本地渲染 HTML/CSS，输出高质量语录图
  * 新增单卡/合集两种渲染布局，支持评论区与群来源标识
  * 头像拉取增加多源回退，提升头像命中率
  * 全局模式下支持跨群语录展示来源群标签

* **⚙️ 配置项扩展与行为统一**
  * 新增/完善：`user_blacklist`、`poke_mode`、`global_mode`、`admin_only`
  * 新增 AI 相关配置：`llm_provider_id`、`system_prompt_template`、`max_history_count`、`max_golden_quotes`
  * 删除权限支持“仅管理员可删”模式
  * 当 `max_golden_quotes=0` 时自动禁用 AI 挖掘功能

---

<details>
<summary>📋 点击查看历史更新日志（可后续补充）</summary>

# [v2.2.2] - 头像支持格式更新
* 修增加对GIF格式头像的支持

# [v2.2.1] - 渲染与兼容性修复
* 修复部分场景下头像/评论渲染异常
* 优化多群与全局模式下来源信息显示

# [v2.2.0] - AI 金句链路初步稳定
* 引入 `/一键金句` 能力
* 增加历史消息回溯与 AI 结果自动入库

# [v2.0.0] - 语录核心重构
* 重构数据层、渲染层与服务层
* 支持批量抽卡、评论展示与跨群模式基础能力

</details>
